/** \brief Pide al usuario un numero flotante y lo devuelve
 *
 * \param Mensaje a mostrar en pantalla
 * \return Un numero flotante
 *
 */

float getFloat (char mensaje [50]);

/** \brief Verifica que el parametro pasado como float contenga un numero entero.
 *
 * \param Numero a verificar
 * \return 1 si es entero, 0 si no lo es.
 *
 */
int esInt (float);
